#!/usr/bin/env sh
# Run the vendored local-llm CLI without requiring a global `npm link`.
# Resolves relative to this script, so it works from any working directory.
set -e
here=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec node "$here/../vendor/src/cli.mjs" "$@"
