diff --git a/scripts/ac-trace.py b/scripts/ac-trace.py
index cbd3b8a..42b0877 100644
--- a/scripts/ac-trace.py
+++ b/scripts/ac-trace.py
@@ -419,7 +419,9 @@ def _walk_candidates(repo: Path) -> list[Path]:
             if child.is_symlink():
                 try:
                     resolved = child.resolve()
-                except OSError:
+                except (OSError, RuntimeError):
+                    # Python <=3.12 raises RuntimeError (not OSError) for a
+                    # symlink loop; 3.13+ resolves it without raising.
                     continue
                 if not _under_repo(resolved, repo):
                     continue
@@ -434,7 +436,9 @@ def _walk_candidates(repo: Path) -> list[Path]:
             if file_path.is_symlink():
                 try:
                     resolved = file_path.resolve()
-                except OSError:
+                except (OSError, RuntimeError):
+                    # Python <=3.12 raises RuntimeError (not OSError) for a
+                    # symlink loop; 3.13+ resolves it without raising.
                     continue
                 if not _under_repo(resolved, repo):
                     continue
